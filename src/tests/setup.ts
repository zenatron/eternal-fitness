import { beforeAll, afterAll } from 'bun:test';

beforeAll(() => {
  console.log('Starting Exercise Library Tests...');
});

afterAll(() => {
  console.log('Exercise Library Tests Complete!');
});

export {};
